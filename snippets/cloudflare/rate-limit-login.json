{
  "description": "Rate limit login attempts - 5 requests per minute",
  "expression": "(http.request.uri.path eq \"/login\" or http.request.uri.path eq \"/api/auth/login\") and (http.request.method eq \"POST\")",
  "action": "block",
  "ratelimit": {
    "characteristics": ["ip.src"],
    "period": 60,
    "requests_per_period": 5,
    "mitigation_timeout": 600
  },
  "enabled": true
}
